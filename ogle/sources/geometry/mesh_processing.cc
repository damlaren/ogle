/**
 * @file mesh_processing.cc
 * @brief Implements mesh_processing.h..
 */

#include "geometry/mesh_processing.h"

void MeshProcessing::ComputeAveragedNormals(Mesh* mesh) {
  // TODO(damlaren): why are mesh verts in a map?
}
